component extends="tests.resources.ModuleIntegrationSpec" appMapping="/app" {

    function run() {
        describe( "PreQuickSave spec", function() {
            it( "announces a preQuickSave interception point", function() {
                fail( "test not implemented yet" );
            } );

            it( "calls any preQuickSave method on the component", function() {
                fail( "test not implemented yet" );
            } );
        } );
    }

    function preQuickSave( event, interceptData, buffer, rc, prc ) {

    }

}
